#include <stdio.h>
int main()
{
    int x = 100;
    while (x --\
                 \
                  \
                   \
                    >0) // x goes to 0
    {
        printf("%d", x);
    }
}